<div class="comment">
  <h4>Comment by <%= comment.author_name %></h4>
  <% if can? :destroy, comment %>
    <%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' }, :id => 'destroy-comments-'+comment.id.to_s %>
  <% end %>
   
  <p><%= comment.body %></p>
</div>
